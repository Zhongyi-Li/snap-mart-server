# Snap Mart Server
# 本文件包含本地开发环境变量（被 gitignore 忽略属正常）。

# HTTP
PORT=3001

# Database
# 本项目 PrismaClient 依赖 DATABASE_URL（见 src/prisma/prisma.service.ts）。
# 使用 docker-compose.yaml 启动 MySQL 后，宿主机访问端口为 3308。
# 需要在宿主机运行 Nest（pnpm start:dev）时：
DATABASE_URL="mysql://root:123456@127.0.0.1:3308/snap_mart"
# 如果未来把 Nest 也放进同一个 docker-compose 网络里运行，通常改成：
# DATABASE_URL="mysql://root:123456@db:3306/snap_mart"

# Auth (JWT)
JWT_SECRET="change_me_to_a_long_random_string"
JWT_EXPIRES_IN=7d
